// Server configuration
const config = {
  // Server URL - change this to your server's URL
  serverUrl: "http://localhost:5000",
  
  // WebSocket URL - change this to your WebSocket server's URL
  wsUrl: "http://localhost:5000",
};

export default config; 